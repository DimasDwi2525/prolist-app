import "./bootstrap";
import MessageModal from "./components/MessageModal";

// Initialize message modal when DOM is loaded
document.addEventListener("DOMContentLoaded", function () {
    new MessageModal();
});
