-   [ ] Change default broadcaster from 'pusher' to 'socketio' in config/broadcasting.php
-   [ ] Create server.js file with Socket.IO server on port 6001 integrated with Redis
-   [ ] Add server dependencies (socket.io, redis, ioredis) to package.json
-   [ ] Update resources/js/bootstrap.js to use Socket.IO instead of Pusher
-   [ ] Ensure Redis configuration is correct
-   [ ] # Test the broadcasting system works end-to-end

## Pending Tasks

-   [x] Change default broadcaster from 'pusher' to 'socketio' in config/broadcasting.php
-   [x] Create server.js file with Socket.IO server on port 6001 integrated with Redis
-   [x] Add server dependencies (socket.io, redis, ioredis) to package.json
-   [x] Update resources/js/bootstrap.js to use Socket.IO instead of Pusher
-   [ ] Ensure Redis configuration is correct
-   [ ] Test the broadcasting system works end-to-end
