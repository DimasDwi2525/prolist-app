# Task: Add Relations to LogController show Method

## Description

Add relations `project.client` and `project.quotation.client.name` to the `show` method in `LogController.php`.

## Steps

-   [x] Modify the `with` array in the `show` method to include `'project.client'` and `'project.quotation.client'`
-   [ ] Verify the changes work correctly

## Files to Edit

-   app/Http/Controllers/API/LogController.php
